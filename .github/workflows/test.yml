on:
  workflow_call:

jobs:
  lint:
    name: "Lint Sources"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: yarn install
        shell: bash
      - run: yarn flow
        shell: bash
      - run: yarn lint
        shell: bash